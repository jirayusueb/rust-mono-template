{
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently \"bun run --filter web dev\" \"cargo run -p api\"",
    "build": "bun run --filter web build && cargo build -p api",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "format": "oxfmt .",
    "format:check": "oxfmt . --check"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "oxfmt": "^0.57.0",
    "oxlint": "^1.72.0"
  }
}
